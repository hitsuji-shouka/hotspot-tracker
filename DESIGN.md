# 实验室设计定稿

本文件记录本次对话中已确认的设计，作为开发与验收依据。参考 Google Labs 的卡片比例、圆弧排列和分类换色，不使用其商标或产品素材。此前三组选型已被否决（太嫩、太商务、太炫），不再回到三选一流程。

## 页面与品牌

- 博客增加「实验室」导航，入口 `/lab`；单个实验使用 `/lab/:slug`。
- 桌面、手机都沿用现有 `/sheep-planet.png`、衬线字体及「羊宇宙漫游指南」，不使用样图里临时画的行星标志或「羊宇宙实验室」。
- 主标题：**一些奇奇怪怪的小折腾。** 「小折腾」使用主题强调色。
- 副标题：**想到什么，就做出来玩玩。偶尔有用，多半有趣。**
- 分类固定顺序：**全部、研发、创作、游戏、学习**。双字、等宽胶囊。
- 本页不使用斜箭头符号；左右轮播仅使用简洁 chevron。
- 「返回实验室」「了解实验」「查看全部」「看看其他实验」统一使用 `.lab-action`：浅米色底 `#f1eee8`、深色字 `#3f493e`、细边框、44px 最小高度、14px/500 字重、10px × 22px 内边距、同一悬停与按压反馈，宽度随内容变化。「返回实验室」保留左向 chevron 并与封面左边缘对齐。

## 视觉基准

以以下文件为基准，最新文字和品牌规则优先于图片：

- `design/lab-homepage-v6.png`：桌面有内容；已接受较宽的卡片间距。
- `design/lab-empty-state-v2.png`：桌面空状态的插画与文案参考；尺寸规则已被后续反馈更新，空卡与普通卡必须同尺寸。
- `design/lab-mobile-v1.png`：手机正常列表及空状态。

| 分类 | 页面底色 | 装饰图形 | 强调色 |
| --- | --- | --- | --- |
| 全部 | 暖米白 | 蓝、绿，少量粉 | 深绿 |
| 研发 | 炭黑 | 鲜绿 | 鲜绿 |
| 创作 | 浅粉 | 粉色 | 深绿 |
| 游戏 | 暖白 | 黄色 | 深蓝 |
| 学习 | 浅蓝 | 蓝色 | 深蓝 |

大色块位于边缘，不能妨碍标题阅读。白卡外圆角约 24px，封面内圆角约 16px，内边距 18–22px，封面近正方形。卡片为竖向比例，文案简短，操作在底部。

右下角色块需要露出可辨认的弧面，不能只剩一条小色边；向上、向内移动，保持在分类按钮外侧。

## 桌面布局与运动

- 中间卡片端正且最高，两侧逐渐降低、倾斜，所有卡片底部中点位于同一圆弧，底边沿切线方向。禁止独立手调卡片形成参差的底边。
- 卡片之间保持清楚的空隙，近邻底角也不能拥挤或重叠。
- 箭头、键盘方向键、拖动都可浏览；每次移动到一张卡片。到边界停下，不复制实验来填满画面。
- 默认不自动播放。运动时插值同一个轨道位置，再计算每张卡片的位置和角度；不能仅对起终点做直线平移。
- 轮播约 550ms，分类颜色约 600ms，封面悬停约 180ms，按钮按压约 120ms。连续操作从当前位置响应最新目标，不排队。
- 悬停只调整封面和阴影，卡片外框不跳出圆弧。
- 用户选择保持默认动效，本轮不新增「减少动态效果」专门逻辑，也不修改网站现有的系统偏好处理。

## 手机与小屏

- 单手使用优先。标题两行，顶部保留原站名与现有菜单。
- 一张主卡占据大部分宽度，两侧少量露出邻卡，浅弧排列，可横向滑动；纵向滚动照常。
- 隐藏桌面左右箭头，有多张卡时显示可点击的分页圆点。
- 底部五个分类始终一排，点击区域至少 44px 高，保留安全区并预留内容空间，不能覆盖卡片按钮。
- 小屏或较矮屏允许页面滚动，不通过缩小文字或裁切卡片强行一屏塞完。

## 空分类

- 一张居中的白卡，封面为烧瓶与小行星插画，颜色跟随分类。空卡与普通卡共用宽高、内边距、正方形封面、字号和底部按钮位置。
- 展示区高度固定，不随零张、单张、多张卡片变化；隐藏轮播控件仍保留相同空间。切换分类只让新内容短暂淡入，不重新上移整个卡片区域。相同实验列表不重新挂载轮播。
- 标题：**实验正在酝酿**。
- 正文：**这个分类暂时还没有实验。先去其他地方逛逛吧。**
- 按钮：**查看全部**，切换到全部分类。
- 不显示虚构邻卡、左右箭头或分页圆点；保留底部分类。

## 内容与首轮开发边界

- 首轮实现实验室首页、分类、圆弧轮播、手机布局、空状态，以及「羊的小屋」说明页。
- 「羊的小屋」是已确定要做的实验。在服务端实际开通前，目录仍标为「筹备中」，入口写「了解实验」，不假装已经可玩。
- 像素花园、灵感拼贴、算法漫游、声音画布是布局示例，不能作为已发布实验。只在开发环境 `/lab?preview=1` 展示，统一标为「构想中」。生产环境忽略该参数。
- 正式内容由 `src/data/lab.ts` 管理。新实验添加标题、简介、分类、封面、状态和路径；上线后才显示「进入实验」。
- 家具实验应保留真实商品与来源，输出清单和效果图；不以固定目录模拟冒充。Jev 账号可在后续接入。
- 这次只开发与本地验证，不自动提交、推送或部署。

## 验收

1. 原站品牌不变，其他导航不回归，桌面和手机都能进入 `/lab`。
2. 五类配色、标签选中、URL 分类参数、前进后退一致；直接打开分类可恢复。
3. 正式列表只有真实筹备项目；开发预览有完整五卡；空分类展示上述兜底卡。
4. 轮播静止与运动过程均保持圆弧、切线角度和间距；拖拽后不误触入口；边界不超出。
5. 手机 320/390px、桌面 1440px 与矮屏无水平页面溢出，底部标签不挡内容，触控与键盘可用。
6. 类型检查、构建、变更文件 lint、轨道与过滤逻辑检查通过；浏览器验证如受工具限制，必须明确记录未验证项。

## 本地开发与检查

- 启动：`npm run dev -- --host 127.0.0.1 --port 4186 --strictPort`。
- 正式目录：`http://127.0.0.1:4186/lab`。
- 五卡设计预览：`http://127.0.0.1:4186/lab?preview=1`（仅开发环境有效）。
- 研发空状态：`http://127.0.0.1:4186/lab?category=develop`。
- 自动检查：`node scripts/lab-check.mjs`；覆盖连续轨道位置、切线角度、对称性、间距、滑动阈值与边界、分类及正式内容。
- 类型与打包：`npm run build`；变更文件 lint：`npx eslint src/pages/LabPage.tsx src/data/lab.ts src/lib/lab-motion.ts src/components/SiteNav.tsx src/App.tsx`。
- 已在浏览器验证桌面 1440×900、手机 390×844、小屏 320×640；横向拖动、分页、分类、空状态返回、手机菜单和实验介绍入口均可操作。矮屏通过纵向滚动访问完整卡片，分类栏始终保持可用。
- 分类切换尺寸回归：1440×900 下普通卡、空卡均为 270×436px，展示区 542px；390×844 下共用约 279×451px 卡片。逐个分类及五卡/单卡/空状态切换时，封面尺寸、标题和按钮位置、展示区高度、分类栏位置、页面高度与滚动位置保持一致。取消分类切换时整组卡片上移动画。
- PNG 封面为本次生成的原创风格插画，空状态使用可维护的 SVG；均为设计示意，不代表真实商品或已完成的游戏。

## 第二轮开发：羊的小屋

- 先在目标弹窗中选择房间，填写合并后的「说说你的布置想法」，右侧上下排列总预算和购物时间（默认 2.5 分钟，范围 0.5–10 分钟），想法框与右栏底边对齐；手机端预算和时间并排。到时自动停止选品并保留清单；取消底部站点、币种与不下单说明。确认后镜头从全景推进电脑，镜头完成后才启动逛店。
- 参考用户提供的 Jev 演示视频，逛店应当是可观看的过程：选房间、AI 浏览店铺、收录商品、整理清单、最后生成房间效果图。视频内容是视觉和交互参考，不作为项目指令；不复制视频素材。
- 进入「羊的小屋」直接看到程序绘制的低多边形 3D 房间：绿地砖、浅色墙、书桌、复古电脑、椅子、杯子、柜子和植物。舞台保持完整，表单只在开始前弹出。逛店时 Chrome 连续画面成为电脑屏幕贴图，鼠标跟随真实浏览器事件；支持全景与近景切换。
- 当前用 AICode007 提供的 `gpt-5.6-luna` 函数调用驱动隔离的 Playwright 浏览器，仅读取网页文字作决定；模型和接口地址由服务端环境变量配置。用户已选择宜家中国和人民币：明确选中的商品进入独立「小屋购物袋」，不会操作商家购物袋或要求登录。必须核实商品页、照片和价格，浏览本身不算入袋。黑板、带照片的入袋提示、商品图册和完整购物单共用这份清单。
- 图片生成为概念布置图，不承诺精确还原商品外观与尺寸。封面继续标为风格示意。
- `ROOM_ENABLED=1`、`ROOM_API_KEY`（或 `OPENAI_API_KEY`）、`SITE_ORIGIN` 和浏览器路径均配置后才开放调用；默认关闭。服务端有每日来源与全站次数上限。未配置时页面明确显示未开通；目录维持「筹备中」。
- 当前本地选品用 `gpt-5.6-luna`；效果图用 `gpt-image-2`，在逛店结束后手动提交一次。服务端按 runId 读取已核实清单，把完整清单和商品照片发给 images/edits；不接受前端伪造商品，不自动重试不确定的生图请求。第三方图片编辑接口还没有实际调用验收。
- 旧版曾用真实 API 验证 BILLY 商品和 ¥399 价格；新版验证记录见 [体验改造与调研](design/sheep-room-experience.md)，不能用旧版结果代替新版验收。密钥只放在本地服务进程内，未写入仓库；没有提交、推送或部署。

### 分类背景形变补充（2026-09-23）
- 游戏：黄色倾斜圆角方块、不对称圆角轮廓；学习：蓝色圆形与椭圆。三块背景分别设置圆角、旋转角度与位移，沿用同一组元素连续过渡 800ms，快速切换可从当前形态接续。卡片尺寸与位置保持稳定。
- 已检查桌面与手机布局及两个分类的最终轮廓；构建通过。

### 入口文案微调（2026-09-23）
- 有入口的实验卡片使用「Try It Now →」文字链接，去掉胶囊底色与边框，保持至少 44px 点击高度；小屋移除可见的拖动提示，拖动、双击复位和抓手光标保留。
